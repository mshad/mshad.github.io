{
  "name": "mshad.github.io",
  "private": true,
  "scripts": {
    "dev": "live-server --host=0.0.0.0 --port=8080 --no-browser --ignore=.git,.idea,node_modules"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
